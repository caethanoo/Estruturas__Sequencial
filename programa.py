valor_por_hora= (float(input("Quanto você ganha por hora?")))
horas_trabalhadas= (float(input("Trabalhou por quantas horas?")))

salário_total = valor_por_hora * horas_trabalhadas

print ("O salário nesse mês é: %.2f" % salário_total)